The Curveball implementation is verified. Commit only the five Flight Recorder files currently shown by git status, with:

git add workflows/codex-flight-recorder
git commit -m "feat(codex): support Curveball transcript format"
git push origin feature/codex-flight-recorder

Then run:
entire checkpoint list
git status -sb

Report the commit hash, checkpoint result, and branch status. Do not merge into main.

---

Run the Flight Recorder using its bundled Track 3 JSONL fixture with affected files:
src/checkout/apply_coupon.ts
tests/checkout/apply_coupon.test.ts

Use the organizer JSONL fixture as historical evidence, show the complete Markdown output, then run:
entire checkpoint list
git log --oneline -3
git status -sb

Do not edit, commit, or push anything.

---

download databricks cli

---

databricks auth login --host "YOUR_WORKSPACE_URL" --profile flight-recorder
databricks current-user me --profile flight-recorder
databricks warehouses list --profile flight-recorder

---

https://dbc-e22dcb6c-79d4.cloud.databricks.com/browse?o=7474649868445029

---

I have installed and authenticated the Databricks CLI with profile `flight-recorder`.

Set up the Codebase Flight Recorder Databricks demo automatically, without changing or committing any Git files.

1. Run `databricks current-user me --profile flight-recorder` and `databricks warehouses list --profile flight-recorder`.
2. If a usable SQL warehouse is listed, use its ID. If none is available, stop and tell me exactly what to create in the Databricks UI; do not create paid resources.
3. Use the Databricks SQL Statement Execution API through the CLI to:
   - create schema `codebase_flight_recorder`;
   - create table `development_history`;
   - insert two clearly labelled SYNTHETIC DEMO DATA rows for `agents/entire-agent-kilo/internal/kilo/hooks.go`, one failed with retries/revert and one passed;
   - create or replace view `component_risk` using the SQL in `workflows/codex-flight-recorder/databricks/flight_recorder_analytics.sql`;
   - run `SELECT * FROM codebase_flight_recorder.component_risk ORDER BY risk_score DESC`.
4. Show me the query result, warehouse ID used, and any dashboard URL if one can be created safely.
5. Never upload source code, prompts, secrets, or personal data.

---

Create one persistent Databricks dashboard for the existing Codebase Flight Recorder demo. Do not modify Git files or upload any data.

Use the already authenticated `flight-recorder` profile and existing `codebase_flight_recorder.component_risk` view.

Create a dashboard titled:
Codebase Flight Recorder — Risk Dashboard

It must include:
1. A bar chart with `file_path` on X and `risk_score` on Y.
2. A table showing `file_path`, `sessions`, `failed_sessions`, `retries`, `reverts`, and `risk_score`.
3. A visible label stating: “SYNTHETIC DEMO DATA — not production telemetry.”

Use CLI/API if supported; otherwise use the Databricks workspace UI. Report the dashboard URL and do not create additional assets.

---

Commit and push only these final judge-demo documentation files:

- BUILDATHON.md
- workflows/codex-flight-recorder/README.md
- workflows/codex-flight-recorder/DEMO.md

Use this commit message:
docs(buildathon): add judge demo runbook

Push only to origin feature/codex-flight-recorder. Then run:
entire checkpoint list
git status -sb

Do not touch main.